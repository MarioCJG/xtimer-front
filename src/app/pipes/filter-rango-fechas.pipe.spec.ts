import { FilterRangoFechasPipe } from './filter-rango-fechas.pipe';

describe('FilterRangoFechasPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRangoFechasPipe();
    expect(pipe).toBeTruthy();
  });
});
